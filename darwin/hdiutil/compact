hdiutil compact: compacts a SPARSE (UDSP) or SPARSEBUNDLE (UDSB) disk image
Usage:	hdiutil compact <image>

	compact options
	    -batteryallowed		allow compacting on battery power
	    -sleepallowed		allow machine to idle sleep (cancels compact)

	Common options:
	    -encryption <crypto method>
	    	AES-128 - 128-bit AES encryption (recommended)
	    	AES-256 - 256-bit AES encryption (more secure, but slower)
	    -stdinpass
	    -agentpass
	    -srcimagekey <key>=<value>	(-imagekey is a synonym)
	    -shadow <shadowfile>
	    -insecurehttp
	    -cacert <file | dir>
	    -plist
	    -puppetstrings
	    -verbose
	    -debug
	    -quiet
