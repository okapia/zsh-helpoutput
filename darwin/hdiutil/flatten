hdiutil flatten: embed any resource fork into UDIF data fork
Usage:	hdiutil flatten <image>
	Flatten options:
	    -xml		embed XML (default)
	    -compressxml		embed compressed XML
	    -rsrcfork	embed resource fork (default)

	Common options:
	    -encryption <crypto method>
	    	AES-128 - 128-bit AES encryption (recommended)
	    	AES-256 - 256-bit AES encryption (more secure, but slower)
	    -stdinpass
	    -agentpass
	    -srcimagekey <key>=<value>	(-imagekey is a synonym)
	    -verbose
	    -debug
	    -quiet
