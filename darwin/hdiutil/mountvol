hdiutil mountvol: mount a volume
Usage:	hdiutil mountvol <devname>
	Options:
	    -whole		mount all partitions on same disk
	    -notimeout		no timeout when mounting
	    -timeout <secs>	mount but timeout after <secs> seconds [default: 30]
	Common options:
	    -plist
	    -verbose
	    -debug
	    -quiet
