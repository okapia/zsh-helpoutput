hdiutil verify: compare data to internal image checksum
Usage:	hdiutil verify <image>
	Options:
	    -[no]cache		do (not) cache checksum-verification [cache]

	Common options:
	    -encryption <crypto method>
	    	AES-128 - 128-bit AES encryption (recommended)
	    	AES-256 - 256-bit AES encryption (more secure, but slower)
	    -stdinpass
	    -agentpass
	    -srcimagekey <key>=<value>	(-imagekey is a synonym)
	    -plist
	    -puppetstrings
	    -verbose
	    -debug
	    -quiet
