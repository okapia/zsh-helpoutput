usage: git replace [-f] <object> <replacement>
   or: git replace -d <object>...
   or: git replace -l [<pattern>]

    -l                    list replace refs
    -d                    delete replace refs
    -f                    replace the ref if it exists

