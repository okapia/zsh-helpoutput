usage: arp [-n] [-c cpu] hostname
       arp [-n] [-c cpu] -a
       arp -d hostname [pub]
       arp -d -a
       arp -s hostname ether_addr [temp] [pub [only]]
       arp -S hostname ether_addr [temp] [pub [only]]
       arp -f filename
